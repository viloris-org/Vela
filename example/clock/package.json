{
  "name": "@vela/example-clock",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Minimal clock app using window.vela (layers + hit); browser mock when host is absent",
  "scripts": {
    "serve": "bun ./serve.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vela/api": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
