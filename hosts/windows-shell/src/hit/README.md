# hit/

Future home of the Windows `resolveHit` mirror and input routing.

- SoT algorithm: `packages/api` `resolveHit`
- Policy state machine: `@vela/shell-core`
- Reference: `hosts/linux-shell/src/hit.zig`, `hosts/desktop-shell/Sources/VelaShell/Hit/`

Not required for scaffold exit.
