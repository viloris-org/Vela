{
  "name": "@vela/template-minimal",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Minimal Vela App scaffold (copy from templates/minimal; not a workspace demo)",
  "scripts": {
    "serve": "bun ./serve.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vela/api": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
