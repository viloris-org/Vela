{
  "name": "@vela/sys-desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Desktop Host systems facades (notify + tray + dialog + clipboard + fs) for linux / macos / windows — T1 inject into HostAPI.sys",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@vela/api": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vela/host-core": "workspace:*",
    "typescript": "^5.8.0"
  }
}
